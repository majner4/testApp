export interface IUserData {
  email?: string;
  _id?: string;
  date?: Date;
  role?: string;
}

export interface IUserInfo {
  firstName?: string;
  lastName?: string;
  age?: number | null;
  id: string;
}
